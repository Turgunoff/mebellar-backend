run:
  timeout: 5m
  skip-dirs:
    - .history

linters:
  disable:
    - dupl
    - errcheck

linters-settings:
  errcheck:
    check-blank: false
  dupl:
    threshold: 200

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0